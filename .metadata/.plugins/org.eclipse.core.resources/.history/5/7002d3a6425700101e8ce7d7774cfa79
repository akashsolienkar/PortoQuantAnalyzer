package com.example.portoquant.Simulation;

/**
 * Functional interface for a stochastic price path generator.
 */
@FunctionalInterface
public interface SimulationRunner
{
	    double[] generate(int steps, double initialPrice, double mu, double sigma, double timeHorizon);
}
